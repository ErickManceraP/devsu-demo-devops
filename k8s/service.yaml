apiVersion: v1
kind: Service
metadata:
  name: devsu-demo-nodejs-svc
  namespace: devsu-demo
  labels:
    app: devsu-demo-nodejs
spec:
  type: ClusterIP
  selector:
    app: devsu-demo-nodejs
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
